<div class="buttons">
  <%= f.button :submit if can?(:modify, main_controller_name) && f.object.updateable? %>
  <%= f.button :back %>

  <% if f.object.persisted? && can?(:destroy, main_controller_name) %>
    <%= f.button :destroy if f.object.destroyable? %>
  <% end %>
</div>

<div class="buttons">
  <% if f.object.waiting? %>
    <%= f.button :submit %>
    <%= f.button :submit, 'Liberar' if f.object.persisted? %>
    <%= f.button :submit, 'Anular', :class => 'negative' if f.object.persisted? %>
  <% end %>

  <%= f.object.decorator.build_licitation_process_link %>
  <%= link_to "Imprimir", resource_path(resource), :class => 'clear button neutral' if f.object.released? %>
  <%= f.button :cancel %>
</div>

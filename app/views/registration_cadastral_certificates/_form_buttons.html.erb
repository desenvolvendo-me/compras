<div class="buttons">
  <%= f.button :submit %>

  <% if f.object.persisted? %>
    <%= f.button :destroy %>
    <%= link_to "Imprimir certificado de registro cadastral", resource_path, :class => 'clear button neutral' %>
  <% end %>

  <%= f.button :back, :href => registration_cadastral_certificates_path(:creditor_id => resource.creditor_id) %>
</div>

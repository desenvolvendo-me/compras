<div class="buttons">
  <%= f.button :submit, 'Sem proposta' %>
  <%= f.button :submit %>
  <%= link_to t(:back), trading_items_path(:trading_id => @parent.trading_id), :class => 'button' %>
  <%= f.button :destroy if f.object.persisted? %>
</div>

<table class="records">
  <thead>
    <tr>
      <% resource_class.decorator.header_attributes.each do |field| %>
        <th><%= resource_class.human_attribute_name field %></th>
      <% end %>
      <th></th>
    </tr>
  </thead>
    <span style="color:#3284B6;"> * O Sistema Permite Cadastrar um Processo de Compras Apenas 1 mas - Caso o Processo de Compras Tenha mais Contratos/Ata Vinculado Ele Faz Todo Gerenciamento na Aba Contratos Vinculados </span>
  <tbody>
    <% collection.each do |resource| %>
      <tr id="<%= dom_id resource %>">
        <% resource_class.decorator.header_attributes.each do |field| %>
          <td>
            <% if resource_class.decorator.link?(field) %>
              <%= link_to(formatted_attribute(resource.decorator, field), edit_resource_path(resource)) %>
            <% else %>
              <%= formatted_attribute(resource.decorator, field) %>
            <% end %>
          </td>
        <% end %>
        <td>
          <%= link_to 'imprimir',contract_conference_path(:contract_id => resource.id), title: 'Imprimir Contrato' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

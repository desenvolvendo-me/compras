<table class="records">
  <thead>
    <tr>
      <th>Número</th>
      <th>Solicitante</th>
      <th>Produto</th>
      <th>Quantidade</th>
      <th>Total</th>
    </tr>
  </thead>

  <tbody>
    <% report.records.each do |record| %>
      <% record.items.each do |item| %>
        <tr>
          <td><%= report.record_code_year(record) %></td>
          <td><%= record.budget_structure %></td>
          <td><%= item.material %></td>
          <td><%= number_with_precision item.quantity %></td>
          <td><%= item.decorator.estimated_total_price %></td>
        </tr>
      <% end %>
    <% end %>
  </tbody>
</table>

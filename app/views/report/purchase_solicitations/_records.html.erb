<table class="records">
  <thead>
    <tr>
      <th>Solicitante</th>
      <th>Produto</th>
      <th>Quantidade</th>
      <th>Total</th>
    </tr>
  </thead>

  <tbody>
    <% report.records_grouped.each do |record| %>
    <tr>
      <td><%= record.budget_structure %></td>
      <td><%= report.item(record) %></td>
      <td><%= number_with_precision record.quantity %></td>
      <td><%= number_with_precision record.total %></td>
    </tr>
    <% end %>
  </tbody>
</table>

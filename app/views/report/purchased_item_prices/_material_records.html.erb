<% report.records.group_by { |r| report.group_by_material r }.each do |material, items| %>
  <div class="field-without-label">
    <table class="records">
      <thead>
        <th>Item</th>
      </thead>

      <tbody>
        <tr>
          <td><%= material %></td>
        </tr>
      </tbody>
    </table>

    <table class="records">
      <thead>
        <th>Processo de compra</th>
      </thead>

      <tbody>
        <% items.group_by { |i| report.send :group_by_licitation, i }.each do |licitation, items| %>
          <tr>
            <td><%= licitation %></td>
          </tr>
      </tbody>
    </table>

    <table class="records">
      <thead>
        <th>Fornecedor</th>
        <th>Valor Unitário</th>
      </thead>

      <tbody>
        <% items.each do |item| %>
          <tr>
            <td><%= item.creditor %></td>
            <td><%= number_to_currency item.unit_price %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <% end %>
  </div>
<% end %>

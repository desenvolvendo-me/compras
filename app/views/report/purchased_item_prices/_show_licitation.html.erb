<% resource.records.group_by { |r| resource.group_by_licitation r }.each do |licitation_process, items| %>
  <h2><%= licitation_process %></h2>

  <% items.group_by { |i| resource.group_by_creditor i }.each do |creditor, items| %>
    <h3><%= creditor %></h3>

    <table class="records">
      <thead>
        <th>Item</th>
        <th>Valor Unitário</th>
      </thead>

      <tbody>
        <% items.each do |item| %>
          <tr>
            <td><%= item.material %></td>
            <td><%= number_to_currency item.unit_price %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% end %>
<% end %>

<div class="yui3-u-3-4">
  <%= f.input :demand, :as => :auto_complete, :fake => true %>
</div>

<%= f.input :purchase_solicitations, as: :nested_grid,
            columns: [:purchase_solicitation, :consumed_value,
                      :expected_value, :resource_source] %>

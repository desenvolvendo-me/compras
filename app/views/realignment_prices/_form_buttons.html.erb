<div class="buttons">
  <% if can?(:modify, main_controller_name) %>
    <%= f.button :submit %>
  <% end %>

  <%= f.button :back, href: realignment_prices_path(purchase_process_id: f.object.purchase_process_id) %>
</div>

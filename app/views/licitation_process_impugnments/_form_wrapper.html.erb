<%= simple_form_for resource.localized, :defaults => { :disabled => !resource.pending? }, :url => resource.persisted? ? resource_path : collection_path, :remote => request.xhr? do |f| %>
  <%= render 'form', :f => f %>
  <%= render 'form_buttons', :f => f %>
<% end %>

<%= simple_form_for resource.localized, :defaults => { :disabled => !resource.pending? }, :url => resource.persisted? ? resource_url : collection_url, :remote => request.xhr? do |f| %>
  <%= render 'form', :f => f %>
  <%= render 'form_buttons', :f => f %>
<% end %>
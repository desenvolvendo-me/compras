<table class="records">
  <thead>
    <tr>
      <th>
        <%= resource_class.human_attribute_name :id %>
      </th>
      <th>
        <%= resource_class.human_attribute_name :ordinance_number %>
      </th>
      <th>
        <%= resource_class.human_attribute_name :validity_beginning %>
      </th>
      <th>
        <%= resource_class.human_attribute_name :published_date %>
      </th>
    </tr>
  </thead>

  <tbody>
    <% localized(collection) do |resource| %>
      <tr id="<%= dom_id resource %>">
        <td>
          <%= resource.id %>
        </td>
        <td>
          <%= link_to resource.ordinance_number, edit_resource_path(resource) %>
        </td>
        <td>
          <%= resource.validity_beginning %>
        </td>
        <td>
          <%= resource.published_date %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

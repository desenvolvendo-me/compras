<%= publish_to @path do %>
    var id = "<%= @conversation.id %>";
    var chatbox = $(".msg-wrap");
    var sender_id = "<%= @message.sender_id %>";

    chatbox.append("<%= j render( partial: 'auction/messages/messages', locals:{ message: @message } ) %>");
<% end %>

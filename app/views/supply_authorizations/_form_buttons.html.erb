<div class="buttons">
  <% if f.object.new_record? %>
    <%= f.button :submit %>
  <% else %>
    <%= link_to "Versão para impressão", resource_path(resource), :class => 'clear button neutral' %>
  <% end %>
  <%= f.button :cancel %>
</div>

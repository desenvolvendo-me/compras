<%= simple_form_for resource.localized, :url => resource.persisted? ? resource_path : collection_path, :remote => request.xhr? do |f| %>
  <%= render 'form_base_erros', :f => f %>
  <%= render 'form', :f => f %>
  <%= render 'form_buttons', :f => f %>
<% end %>

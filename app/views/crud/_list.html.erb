<table class="records">
  <tbody>
    <% collection.each do |resource| %>
      <tr id="<%= dom_id resource %>">
        <td>
          <p><%= link_to resource, edit_resource_path(resource) %></p>

          <% if resource.presenter? && resource.presenter.summary? %>
            <p><%= resource.presenter.summary %></p>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

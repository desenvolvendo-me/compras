<p>
  <%= link_to 'Encerrar pregão', new_trading_closing_path(:trading_id => resource.id), :class => "button primary" %>
<p>

<%= render 'trading_closings/list', :resource_class => TradingClosing, :collection => resource.closings %>

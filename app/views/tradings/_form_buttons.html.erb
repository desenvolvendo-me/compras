<div class="buttons">
    <% if f.object.persisted? %>
      <%= f.button :destroy %>
      <%= link_to('Licitantes', bidders_path(:licitation_process_id => resource.licitation_process.id), :class => "button primary") %>
      <%= link_to('Itens/Ofertas', trading_items_path(:trading_id => resource.id), :class => 'button primary') %>
    <% end %>
    <%= f.button :submit %>
    <%= f.button :back %>
</div>

<div class="buttons">
  <%= f.button :submit %>
  <%= link_to('CRC', registration_cadastral_certificates_path(:creditor_id => resource.id), :class => "button primary") if f.object.persisted? %>
  <%= f.button :cancel %>
  <%= f.button :destroy if f.object.persisted? %>
</div>

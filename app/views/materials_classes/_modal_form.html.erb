<% attributes.each do |attribute| %>
  <%= f.input attribute, :required => false, :hint => false %>
<% end %>

<script>
  var group_name = $('#material_materials_group').attr('value'),
      group_id   = $('#material_materials_group_id').attr('value');
  $('#filter_materials_group').val(group_name);
  $('#filter_materials_group_id').val(group_id);
  $('#filter_materials_group').attr('disabled', true);
</script>

<%= simple_form_for licitation_process_object, url: update_path, method: :put do |f| %>

  <%= render 'crud/form_base_erros', :f => f %>
  <%= render 'form', :f => f %>
  <%= render 'form_buttons', :f => f %>

<% end %>

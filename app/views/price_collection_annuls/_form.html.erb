<%= f.input :price_collection_id, :as => :hidden %>

<div class='yui3-g'>
  <div class='yui3-u-3-5'>
    <%= f.input :employee, :disabled => !f.object.new_record?%>
  </div>

  <div class='yui3-u-1-5'>
    <%= f.input :date, :disabled => !f.object.new_record? %>
  </div>

  <div class='yui3-u-1-5'>
    <%= f.input :annul_type, :disabled => !f.object.new_record? %>
  </div>
</div>

<%= f.input :description, :disabled => !f.object.new_record? %>

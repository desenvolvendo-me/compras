<%= f.input :annullable, :as => 'hidden' %>

<div class='yui3-g'>
  <div class='yui3-u-2-3'>
    <%= f.input :employee, :disabled => f.object.persisted? %>
  </div>

  <div class='yui3-u-1-3'>
    <%= f.input :date, :disabled => f.object.persisted? %>
  </div>
</div>

<%= f.input :description, :disabled => f.object.persisted? %>

<%= f.input :licitation_process_id, :as => :hidden %>

<%= f.input :purchase_process_accreditation_creditors, as: :nested_grid,
  columns: [:creditor, :personable_type, :company_size, :creditor_representative, :kind_text, :has_power_of_attorney_text],
  exclude_data: [:personable_type, :has_power_of_attorney_text, :kind_text],
  extra_data: [:has_power_of_attorney, :kind] %>

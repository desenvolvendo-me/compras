.ui-modal-loading {
  background: url(<%= image_path 'ajax-loader.gif' %>) no-repeat;
  display: block;
  height: 16px;
  left: 50%;
  margin-left: -8px;
  margin-top: -8px;
  position: absolute;
  top: 50%;
  width: 16px;
}
